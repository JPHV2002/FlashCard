export type DeckParms = {
    userId: string,
    deckName: string,
    deckId: string
}

export type DeckInfo = {
    deckName: string,
    deckId: string
}

export type getDeckParms = {
    userId: string,
    deckId: string
}

export type deckUpdate = {
    deckName: string
}